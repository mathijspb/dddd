import SectionComponent from '../SectionComponent/index.vue';

import data from '../../data/components';

export default {
    components: {
        SectionComponent,
    },

    data() {
        return {
            data,
        };
    },
};
