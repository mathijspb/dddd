import Section from '../Section/index.vue';

import data from '../../data/components';

export default {
    components: {
        Section,
    },

    data() {
        return {
            data,
        };
    },
};
