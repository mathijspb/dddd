import Section from '../Section/index.vue';

import data from '../../data/setup';

export default {
    components: {
        Section,
    },

    data() {
        return {
            data,
        };
    },
};
