import Section from '../Section/index.vue';

import data from '../../data/layout';

export default {
    components: {
        Section,
    },

    data() {
        return {
            data,
        };
    },
};
